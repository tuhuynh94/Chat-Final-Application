<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background_app"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.thuong.tu.chatapplication.yolo.frontend.activities.login.RegisterActivity">

    <EditText
        android:id="@+id/edit_password"
        style="@style/input_field"
        android:layout_marginTop="28dp"
        android:background="@drawable/password_input"
        android:ems="12"
        android:hint="Password"
        android:inputType="textPassword"
        android:textAlignment="center"
        android:layout_above="@id/edit_confirm_password"
        android:layout_centerInParent="true"/>

    <EditText
        android:id="@+id/edit_confirm_password"
        style="@style/input_field"
        android:layout_marginBottom="8dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/password_input"
        android:ems="12"
        android:hint="Confirm password"
        android:inputType="textPassword"
        android:textAlignment="center"
        android:textSize="17dp"
        android:layout_above="@id/btn_sign_in"
        android:layout_centerInParent="true"/>

    <Button
        android:id="@+id/btn_sign_in"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:background="@drawable/button_app"
        android:fontFamily="sans-serif"
        android:text="REGISTER"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp"
        android:layout_centerInParent="true"/>

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginTop="5dp"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        app:srcCompat="@drawable/logo_app" />

</RelativeLayout>
